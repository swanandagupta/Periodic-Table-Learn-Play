if(startLevel(level) = 2){
    alert(`Starting Level ${level}`);
        // Add link to respective level in your other project
        window.location.href = `game2.html`;
}
else{
    function startLevel(level) {
        alert(`Starting Level ${level}`);
        // Add link to respective level in your other project
        window.location.href = `game1.html`;
    }
}